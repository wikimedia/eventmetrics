{% for column in ['diff', 'date', 'page', 'wiki', 'username', 'edit-summary'] %}
{{ msg(column) }}{% if not loop.last %},{% endif %}
{% endfor %}

{% for rev in revisions %}
"https://{{ rev.wiki }}.org/wiki/Special:Diff/{{ rev.id }}",{{ rev.timestamp|date_format }},"{{ rev.page }}",{{ rev.wiki }},"{{ rev.username }}","{{ rev.summary }}"
{% endfor %}
