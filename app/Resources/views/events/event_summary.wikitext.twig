{% import 'macros/wiki.html.twig' as wikiHelper %}
=== {{ msg('event-summary') }} &ndash; {{ event.displayTitle }} ===
<small>Data snapshot as of {{ event.updatedWithTimezone|date_localize }} ({{ event.displayTimezone }})</small>

{| class="wikitext"
|-
| {{ msg('participants') }}
| {{ event.numParticipants }}{#-
-#}{% for metric in ['participants', 'pages-created', 'pages-improved', 'byte-difference', 'files-uploaded', 'items-created', 'items-improved', 'pages-created-pageviews', 'pages-improved-pageviews'] %}
{% set stat = event.getStatistic(metric) %}
{% if stat is not empty %}

|-
| {{ msg(metric, [stat.offset]) }}
| {% if stat.value is null %}&ndash;{% else %}{{ stat.value|num_format }}{% endif %}
{% endif %}
{% endfor %}

|}
